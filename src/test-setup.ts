// -------------------------------------------------------------------------------------
// Конфигурација за тестове
// -------------------------------------------------------------------------------------
//
// У прегледачу је уписује `index.html` пре bundle-а; овде исто, пре него што иједан тест
// увезе модул. Празан `basePath` и `apiUrl` значе да тестови виде путање онакве какве их
// пише сам модул, без префикса инстанце.

;(globalThis as { __RUNTIME_CONFIG__?: unknown }).__RUNTIME_CONFIG__ = {
  basePath: '/',
  apiUrl: '',
  pismo: 'cirilica',
  environment: 'test',
}
